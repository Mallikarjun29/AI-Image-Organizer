global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'AI Image Organizer'
    static_configs:
      - targets: ['app:8001']  # Our FastAPI app's Prometheus metrics endpoint
    metrics_path: '/metrics'